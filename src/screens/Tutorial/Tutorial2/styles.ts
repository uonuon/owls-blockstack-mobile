import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  textContainer: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  description1: {
    fontSize: 16,
    marginTop: 50,
  },
  subDescription1: {
    fontSize: 20,
  },
  description2: {
    fontSize: 20,
  },
});

export default styles;
